# put all pods into modules
use_frameworks!
 
target 'Boarded' do
  platform :ios, '10.0'
  pod 'Alamofire', '~>4.0'
  # pod 'MobileVLCKit'
  pod 'MagicalRecord'
  # pod 'SwiftRecord', :git => 'https://github.com/rae/SwiftRecord.git'
  # pod 'SwiftRecord', :path => '../SwiftRecord'
end
